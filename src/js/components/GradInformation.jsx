import React from "react";

export const GradInformation = ({id, title, body}) => {
    return (
        <div className="modal fade" id={id} tabIndex="-1" aria-labelledby={`${id}Label`} aria-hidden="true">
        <div className=""> </div>
        </div>
    )
}